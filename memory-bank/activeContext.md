# Active context

## Current focus

**NavSecondaryCarousel / Market arc menu** — geometry must match `docs/04-operations/dev/nav-secondary-carousel.md` (wide shallow arc, ≥4 visible neighbors on Market at 390px, moderate flank rotation).

## Mandatory reads before UI work

1. `docs/04-operations/dev/AGENTS.md` (especially §0 and §8)
2. `docs/04-operations/dev/nav-secondary-carousel.md` when touching secondary nav or Market bottom carousel
3. `memory-bank/systemPatterns.md` for component map

## Verification

After front-end changes: `npm run verify:post-dev`
